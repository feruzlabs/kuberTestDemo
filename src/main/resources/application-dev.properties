# Dev profile — demo.feruzlabs.dev/kube-test-1 da deploy qilinganda
# Reverse proxy /kube-test-1 prefix — Swagger UI config va spec path lari
springdoc.swagger-ui.config-url=/kube-test-1/api-docs/swagger-config
springdoc.swagger-ui.url=/kube-test-1/api-docs

# Forward headers — X-Forwarded-Prefix (/kube-test-1) ni qabul qilish uchun
server.forward-headers-strategy=framework
