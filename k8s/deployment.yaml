apiVersion: apps/v1
kind: Deployment
metadata:
  name: kuber-test-demo
  labels:
    app: kuber-test-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kuber-test-demo
  template:
    metadata:
      labels:
        app: kuber-test-demo
    spec:
      containers:
        - name: app
          # Docker Hub: docker.io/<USERNAME>/kuber-test-demo:latest
          # GHCR: ghcr.io/<ORG>/kuber-test-demo:latest — build/push qiling, keyin shu image ni yozing
          image: docker.io/feruzlabs/kuber-test-demo:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8010
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
            - name: SERVER_PORT
              value: "8010"
          startupProbe:
            httpGet:
              path: /api-docs
              port: 8010
            initialDelaySeconds: 15
            periodSeconds: 10
            timeoutSeconds: 15
            failureThreshold: 18
          livenessProbe:
            httpGet:
              path: /api-docs
              port: 8010
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /api-docs
              port: 8010
            periodSeconds: 5
            timeoutSeconds: 5
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
